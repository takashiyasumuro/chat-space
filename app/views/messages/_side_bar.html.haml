.Side-bar
  .Side-bar__header
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        =link_to '#' do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')
  .Side-bar__groups
    .side-box
      .side-title
        techcamp
      .side-message
        まだメッセージがありません